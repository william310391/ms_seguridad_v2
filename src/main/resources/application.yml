spring:
    application:
        name: msvc-course
    config:
        import: optional:configserver:http://localhost:8888       

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
  paths-to-match: "/**"


##http://localhost:8090/swagger-ui.html

